# ── Secrets & configuration locale (NE JAMAIS committer) ─────────────────────
.env
.env.*
!.env.example

# ── Données générées à l'exécution (bases, journaux, rendus, sorties) ─────────
# output/ contient studio.db (historique) ET securite.db (mots de passe hachés,
# comptes) + les journaux et les rendus. Rien de tout cela ne va sur GitHub.
output/

# Logs des lancements de l'API
lancements_api/

# ── Build mobile APK (Capacitor) ─────────────────────────────────────────────
mobile-apk/android/
mobile-apk/node_modules/
mobile-apk/dist/
mobile-apk/*.keystore
mobile-apk/*.jks

# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
